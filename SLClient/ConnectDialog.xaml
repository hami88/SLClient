<Window x:Class="SLClient.ConnectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Verbinden" Height="150" Width="300" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" VerticalAlignment="Center">Host:</Label>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="HostBox" Margin="5" Text="mud.silberland.at"/>

        <Label Grid.Row="1" Grid.Column="0" VerticalAlignment="Center">Port:</Label>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="PortBox" Margin="5" Text="4711"/>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Width="75" Margin="5,0" Content="Verbinden" IsDefault="True" Click="Connect_Click"/>
            <Button Width="75" Margin="5,0" Content="Abbrechen" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>